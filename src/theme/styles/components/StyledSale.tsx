import { styled } from 'styled-components';
import tw from 'twin.macro';

export const StyledSale = styled.div`
  ${tw`flex items-center justify-center	w-[95px] h-7 text-main-white bg-main-red rounded-r`}
`;
